import React from "react";
import "./Skills.scss";

export default function Skills() {

  // Create an array with all the names of the items I want to load
  // have a pre-text to the whole situation then include the string element
  // Have it run as a function
  // E.g. icon-android.svg --> icon-{arr[i]}.svg

  return (
    <>
      <h3>Looking forward to working with</h3>

      <h3>The tools that I am familar with</h3>
      
    </>
  );
}
